version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./output:/app/output
    environment:
      - SHOW_TOPIC=Artificial Intelligence
    restart: unless-stopped
